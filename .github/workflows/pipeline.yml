name: 'Output buildings'
on:
  push:
    branches:
      - master
jobs:
  default:
    runs-on: ubuntu-latest
    container:
      image: python:3.11
    steps:
      - name: Install dependencies
        run: pip install geopandas==0.14
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run script
        run: python main.py
      - uses: actions/upload-artifact@v4
        with:
          name: buildings.csv
          path: output/buildings.csv
